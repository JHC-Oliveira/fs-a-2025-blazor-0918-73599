@page "/counter"
@rendermode InteractiveServer

<PageTitle>Counter</PageTitle>

<h1>Counter</h1>

<p role="status">Current count: @currentCount</p>
@if (!string.IsNullOrEmpty(myName))
{
    <p role="status">You clicked @myName</p>
}

<button class="btn btn-primary" @onclick="IncrementCount">Increment</button>
<button class="btn btn-primary" @onclick="DecrementCount">Decrement</button>
@if (ShowName)
{
	<button class="btn btn-primary" @onclick="SetName">Show Name</button>
}

@code {
    [Parameter]
	public int IncrementAmount { get; set; } = 1;
	[Parameter]
	public int DecrementAmount { get; set; } = 1;
	[Parameter]
	public bool ShowName { get; set; } = true;

    private int currentCount = 0;
    private string myName = "";

    private void IncrementCount()
    {
        currentCount += IncrementAmount;
    }
    private void DecrementCount()
    {		
        if (currentCount > 0){
            currentCount -= DecrementAmount;
        }
    }
    private void SetName()
    {
        myName = "Joao Henrique";
    }
}
